% Adds the `\toprule`, the `\midrule`,
% and the `\bottomrule` commands for
% prettier `\hline`s for tables
\usepackage{booktabs}

\append{toc}
\append{listings}
\append{figures}
\append{bibliography}
\append{fonts}
