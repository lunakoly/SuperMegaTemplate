% Adds the `\toprule`, the `\midrule`,
% and the `\bottomrule` commands for
% prettier `\hline`s for tables
\usepackage{booktabs}

\append{bibliography}
\append{listings}
\append{figures}
\append{tables}
\append{appendix}
\append{toc}

\append{attachments}
\append{fonts}
