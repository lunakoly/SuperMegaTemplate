\providecommand\biblatexstyle{numeric}
\providecommand\biblatexsorting{nty}

% Option `defernumbers = true` means
% the bib entries may have "randomly" ordered
% numbers on the right.

\usepackage[
    backend = biber,
    hyperref = true,
    % defernumbers = true,
    style = \biblatexstyle,
    sorting = \biblatexsorting,
]{biblatex}

\newcommand{\appendbibliography}[1]{
    \addbibresource{\pwd#1}
}
