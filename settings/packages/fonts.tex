\usepackage{fontspec}

\setmonofont{FiraCode}[
    Contextuals = Alternate,
	SizeFeatures = {
	    Size = 10,
    },
	Path = \fonts,
	Extension = .ttf,
    UprightFont = *-Regular,
]

% Allows the use of FiraCode's ligatures within verbatim
\usepackage[verbatim]{lstfiracode}
