% Adds the `\toprule`, the `\midrule`,
% and the `\bottomrule` commands for
% prettier `\hline`s for tables
\usepackage{booktabs}

% Adds `\multirow` & \multicolumn
\usepackage{multirow}

\usepackage{tabularx}

\newenvironment{fixedtable}[1]{%
    \begin{fixed}{table}{#1}%
}{%
    \end{fixed}%
}

\newenvironment{floatingtable}[1]{%
    \begin{floating}{table}{#1}%
}{%
    \end{floating}%
}
